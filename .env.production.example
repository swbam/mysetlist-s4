# =============================================================================
# PRODUCTION DEPLOYMENT ENVIRONMENT VARIABLES
# =============================================================================
# These variables MUST be configured in Vercel Environment Variables
# DO NOT commit actual values to the repository
# =============================================================================

# =============================================================================
# APPLICATION CONFIGURATION (REQUIRED)
# =============================================================================
# These MUST be set to your actual domain in Vercel
NEXT_PUBLIC_URL=https://theset.live
NEXT_PUBLIC_APP_URL=https://theset.live
NEXT_PUBLIC_WEB_URL=https://theset.live
NEXT_PUBLIC_API_URL=https://theset.live/api
NODE_ENV=production

# =============================================================================
# SUPABASE CONFIGURATION (REQUIRED)
# =============================================================================
# Get these from your Supabase project settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

# Database connection (from Supabase Database Settings)
DATABASE_URL=postgresql://postgres:[password]@[host]:6543/postgres?pgbouncer=true&connection_limit=1

# =============================================================================
# EXTERNAL API KEYS (REQUIRED FOR FUNCTIONALITY)
# =============================================================================
# Spotify Developer Dashboard: https://developer.spotify.com/
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
NEXT_PUBLIC_SPOTIFY_CLIENT_ID=your_spotify_client_id

# Ticketmaster Developer Portal: https://developer.ticketmaster.com/
TICKETMASTER_API_KEY=your_ticketmaster_api_key

# Setlist.fm API: https://api.setlist.fm/docs/1.0/index.html
SETLISTFM_API_KEY=your_setlistfm_api_key

# =============================================================================
# AUTHENTICATION & SECURITY (REQUIRED)
# =============================================================================
# Generate a 32+ character random string
NEXTAUTH_SECRET=your_32_character_minimum_secret_string
NEXTAUTH_URL=https://your-domain.vercel.app

# Generate secure random strings for these
CSRF_SECRET=your_csrf_secret_key
CRON_SECRET=your_secure_cron_secret

# Comma-separated list of admin user IDs (get from Supabase auth.users table)
ADMIN_USER_IDS=admin_user_id_1,admin_user_id_2

# =============================================================================
# ANALYTICS & MONITORING (OPTIONAL)
# =============================================================================
# PostHog Analytics: https://posthog.com/
NEXT_PUBLIC_POSTHOG_KEY=your_posthog_project_api_key
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Sentry Error Tracking: https://sentry.io/
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_url
SENTRY_ORG=your_sentry_organization
SENTRY_PROJECT=your_sentry_project

# =============================================================================
# EMAIL SERVICES (OPTIONAL)
# =============================================================================
# Resend Email Service: https://resend.com/
RESEND_API_KEY=your_resend_api_key
EMAIL_SYSTEM_TOKEN=your_email_system_token

# =============================================================================
# CACHING & PERFORMANCE (OPTIONAL)
# =============================================================================
# Upstash Redis: https://upstash.com/
UPSTASH_REDIS_REST_URL=your_upstash_redis_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token

# Vercel Edge Config
EDGE_CONFIG=your_vercel_edge_config_url

# =============================================================================
# FEATURE FLAGS (OPTIONAL)
# =============================================================================
# Production feature toggles
NEXT_PUBLIC_ENABLE_SPOTIFY=true
NEXT_PUBLIC_ENABLE_REALTIME=true
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_ENABLE_PERFORMANCE_MONITORING=true

# =============================================================================
# PERFORMANCE BUDGETS
# =============================================================================
# Lighthouse performance targets
PERFORMANCE_BUDGET_LCP=2500
PERFORMANCE_BUDGET_FCP=1800
PERFORMANCE_BUDGET_CLS=0.1

# =============================================================================
# DEPLOYMENT VERIFICATION
# =============================================================================
# Set these to verify deployment is working
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_DEPLOYMENT_ENV=vercel

# =============================================================================
# INSTRUCTIONS FOR VERCEL DEPLOYMENT
# =============================================================================
# 1. Go to your Vercel project settings
# 2. Navigate to Environment Variables
# 3. Add each required variable above with your actual values
# 4. Set Environment to "Production" for production-only variables
# 5. Set Environment to "Preview" and "Development" for variables needed in all environments
# 6. Deploy your application
# 
# SECURITY NOTES:
# - Never commit actual API keys or secrets to the repository
# - Use Vercel's environment variable encryption
# - Regularly rotate API keys and secrets
# - Use different credentials for development and production